
 find . -size 1k  -exec file '{}' \;

;;调用朗读软件朗读选区
(defun langdu-espeak(start end)
  (interactive "r")
  (let ((content (buffer-substring start end)))
    ;; (start-process NAME BUFFER PROGRAM &rest PROGRAM-ARGS)
    (start-process "espeak" nil "espeak" "-vcmn" "-s80"  "-a50" content)
    ))

